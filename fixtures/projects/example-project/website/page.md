This bundled fixture is a small, reviewable project package. Its structured
manifest keeps maturity, capability state, and roadmap intent separate so the
consumer can present each claim honestly.

The fixture is disabled in the production registry and is used by importer
tests. Read more at [the example repository](https://github.com/example/example-project).
