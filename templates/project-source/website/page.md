Replace this body with the project page's approved Markdown. This file is body
only: do not add YAML/TOML frontmatter or repeat facts that belong in
`project.json`.

## Context

Use prose for context, decisions, and details that the project owner has
verified. Keep unverified plans in the manifest's `roadmap` with an explicit
state instead of presenting them as current capabilities.
